extern printf(fstr: string, varargs): i32
extern scanf(fstr: string, varargs): i32

def change(a: ptr i32) {
	#printf("a = %d", deref a)
}

def main(): i32 {
	printf("huh\n")

	let a = 5
	#let b = ref a
	change(ref a)

	return 0
}
