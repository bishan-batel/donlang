# Imported C Methods


extern puts(str: string): i32
extern putc(str: char): i32
extern scanf(str: string, a: string): i32

def main(): i32 {
    let a = 10d
    let b = 20d

    if a > b {
        puts("a is greater than b")
    } 

    return 0
}
