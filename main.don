# extern C functions
extern puts(str: string): i32
extern printf(str: string, f: i32): i32
extern scanf(str: string, a: ptr f64): i32

def main(): i32 {

    let a = 69

    while 1 {
        #printf("%d\n", 69)
        printf("%d\n", a)
        let b = a 
        #a = a + 1
    }

    return 0
}
