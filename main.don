extern printf(fstr: string, varargs): i32
extern scanf(fstr: string, varargs): i32
extern malloc(size: i32): ptr i32
extern free(p: ptr i32)

class Person {
	pub name: string
	pub age: i32

	def speak() {
		self.bruh()
		printf("Hello, my name is %s and I am %d years old"#:TODO:#)
	}

	static def bruh() {
		printf("huh\n")
	}
}

def main(): i32 {
	Person.bruh()
	
	let self = ""
	return 0
}
