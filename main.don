# main method (args parameter optional)


extern printf(msg: string)
extern scanf(scanstr: string, ptr: i32): string

def squared(num: f32): void {
	let val = num * num
	return val
}

def main() {
	printf("Hello World")

	let input = 0
	scanf("%d", input)

	if input > 0 {
		printf("Input is positive")
	} else {
		printf("Input is negative")
	}

	#var a = 0

	return squared(input)
}
